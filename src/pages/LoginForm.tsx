import React from 'react';
import LoginContainer from '../containers/auth/containers/LoginContainer';
const LoginForm = () => {
    return (
        <LoginContainer />
    );
};

export default LoginForm;